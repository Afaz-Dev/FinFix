Custom App Logo
----------------

Place a square PNG named `logo.png` in this folder to override the default generated FinFix logo.

Recommendations:
- Size: 256×256 or larger
- Background: transparent preferred
- Safe area: keep content within 88% of bounds (circular mask)

The application will automatically load `assets/logo.png` if present; otherwise, it falls back to a generated logo.

